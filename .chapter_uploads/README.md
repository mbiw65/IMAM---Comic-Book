# Corrected chapter upload staging

Upload exactly these three archives into this folder:
- issue1.zip
- issue2.zip
- issue3.zip

The GitHub Action will unpack them, preserve the supplied WEBP artwork, normalize page order, generate Cloudflare-compatible PDFs, rebuild all three readers, and publish the site automatically.
